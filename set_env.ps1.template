# set_env.ps1.template - Azure Developer CLI 環境變數設定範本
# 複製此檔案為 set_env.ps1 並填入你的實際值
# 注意：set_env.ps1 應該加入 .gitignore，不要提交到版本控制

Write-Host "設定 Azure Developer CLI 環境變數..." -ForegroundColor Green

# Azure 基本配置
azd env set AZURE_LOCATION 'eastus'                          # Azure 區域
azd env set AZURE_SUBSCRIPTION_ID 'your-subscription-id'     # 你的 Azure 訂閱 ID
azd env set AZURE_TENANT_ID 'your-tenant-id'                 # 你的 Azure Tenant ID
azd env set resourceGroupName 'your-resource-group'          # 資源群組名稱

# Azure OpenAI 配置
azd env set AZURE_OPENAI_API_KEY 'your-openai-api-key'       # OpenAI API 金鑰
azd env set AZURE_OPENAI_SERVICE 'your-openai-service'       # OpenAI 服務名稱
azd env set AZURE_OPENAI_RESOURCE_GROUP 'your-resource-group'
azd env set AZURE_OPENAI_CHATGPT_DEPLOYMENT 'gpt-4o'         # Chat 模型部署名稱
azd env set AZURE_OPENAI_EMBEDDINGS_DEPLOYMENT 'text-embedding-ada-002'
azd env set openAiServiceName 'your-openai-service'

# Azure AI Search 配置
azd env set AZURE_SEARCH_SERVICE 'your-search-service'       # AI Search 服務名稱
azd env set AZURE_SEARCH_KEY 'your-search-api-key'          # AI Search API 金鑰
azd env set AZURE_SEARCH_LOCATION 'eastus'                   # Search 服務區域
azd env set AZURE_SEARCH_SERVICE_RESOURCE_GROUP 'your-resource-group'

# Azure Video Indexer 配置
azd env set AZURE_VIDEO_INDEXER_ACCOUNT_NAME 'your-vi-account'
azd env set AZURE_VIDEO_INDEXER_RESOURCE_GROUP 'your-resource-group'

# 應用程式配置
azd env set LANGUAGE_MODEL 'openai'
azd env set PROMPT_CONTENT_DB 'azure_search'
azd env set PROMPT_CONTENT_DB_NAME 'vi-production-index'

Write-Host "環境變數設定完成！" -ForegroundColor Green
Write-Host "請確認所有設定值都正確填寫" -ForegroundColor Yellow